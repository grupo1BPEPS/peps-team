apiVersion: apps/v1
kind: Deployment
metadata:
  name: apache
  namespace: grupob1
spec:
  replicas: 1
  selector:
    matchLabels:
      app: apache
  template:
    metadata:
      labels:
        app: apache
    spec:
      containers:
        - name: apache
          image: sirjousis/pepsapache:1.0
          ports:
            - containerPort: 80
